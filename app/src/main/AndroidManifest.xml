<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kugrocery">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CheckoutActivity"></activity>
        <activity android:name=".ProductHome" />
        <activity
            android:name=".AddProduct"
            android:label="Add Product"
            android:parentActivityName=".CashierSignUp" />
        <activity
            android:name=".CashierSignUp"
            android:label="Cashier SignUp"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CashierSignIn"
            android:label="Cashier Login"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>